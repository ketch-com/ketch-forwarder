#!/usr/bin/env sh

# Code generated by shipbuilder init 1.19.1. DO NOT EDIT.

if [ ! -f "./scripts/check.sh" ]; then
  cd $(command dirname -- "$(command readlink -f "$(command -v -- "$0")")")/..
fi

. ./scripts/check.sh

if [ -f "package.json" ]; then
  check npm

  set -e

  $npm install
  $npm run build
else
  if [ -n "$(find . -regex ".*node_modules.*" -prune -o -name package.json -print)" ]; then
    check npm

    set -e

    find . -regex ".*node_modules.*" -prune -o -name package.json -execdir $npm install \; -execdir $npm run build \; -print
  fi
fi
