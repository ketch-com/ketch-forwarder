# openapi

The OpenAPI specification for the gateway is contained within this folder.

## Using

Edit the files in `components`, `paths` and `servers` to define your OpenAPI services. Do not
modify the `*_gen.yaml` files as these are generated and will be overwritten.
